interface:
  display_name: "Slot 原版数值对齐 v4.5"
  short_description: "开工前确认Runtime与样本，再持续完成FORMAL对齐"
  default_prompt: "使用 $slot-alignment 先列举并确认Runtime候选与原版样本范围，再以默认确定性多Worker分片完成FORMAL对齐与交付。"
policy:
  allow_implicit_invocation: false
