interface:
  display_name: "Slot 原版体验对齐 v6.0.0"
  short_description: "完成独立FORMAL验收并生成单一中文对齐报告"
  default_prompt: "使用 $slot-alignment 完成原版证据审计、自动对齐和独立FORMAL验收，并生成唯一的人类可读对齐报告.md。"
policy:
  allow_implicit_invocation: false
