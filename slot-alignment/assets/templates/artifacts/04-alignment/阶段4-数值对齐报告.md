# 阶段4-数值对齐报告

## 首页结论

- 展示方式：两列表格汇总最终FORMAL结论和交付建议。
- 必需字段：项目、最终结果
- 空值规则：FORMAL无效时不得显示可进入交付。
- 展示实例：
```markdown
| 项目 | 最终结果 |
|---|---|
| 对齐结论 | 通过 |
```

### 一句话结论

- 展示方式：一句话说明最终状态及证据边界。
- 必需字段：对齐结论
- 空值规则：无法判定时明确等待补证。
- 展示实例：
```markdown
本次对齐结论为通过；以本报告后续FORMAL证据为准。
```

## 1. 任务范围与依据

- 展示方式：章节容器，依次展示范围、资料和统计口径。
- 必需字段：无独立字段（由1.1至1.3子章节组成）
- 空值规则：任一子章节缺失即报告不完整。
- 展示实例：
```markdown
本章固定FORMAL对应的任务范围、资料版本和统计口径。
```

### 1.1 对齐范围

- 展示方式：范围摘要与目标RTP详情分开展示。
- 必需字段：字段、值、边界、目标项
- 空值规则：游戏、模式、RTP组或目标缺失即无法判定。
- 展示实例：
```markdown
| 字段 | 值 | 边界 |
|---|---|---|
| 模式 | feature_buy | 不得跨作用域 |

| 目标项 | 值 |
|---|---|
| 目标RTP | 0.96 |
```

### 1.2 权威资料与版本

- 展示方式：资料摘要只引用ID，长路径和Hash放入独立表；样本能力政策用全局摘要表展示一次。
- 必需字段：资料ID、资料、资格、路径、SHA-256/版本、样本能力政策项、值、说明
- 空值规则：关键资料缺失或Hash无效即阻塞。
- 展示实例：
```markdown
| 资料ID | 资料 | 资格 |
|---|---|---|
| P01 | Runtime | 已密封 |

| 资料ID | 路径 | SHA-256/版本 |
|---|---|---|
| P01 | /abs/runtime.json | abc… |
| POL01 | assets/policies/sample_capability_policy.v1.json | def… |

| 样本能力政策项 | 值 | 说明 |
|---|---|---|
| 政策状态 | 通过 | FORMAL按实际逐指标、逐条件组复验 |
```

### 1.3 统计口径

- 展示方式：两列表格逐项说明FORMAL统计口径。
- 必需字段：口径项、定义
- 空值规则：投注、派奖或完整入口定义缺失即无法验收。
- 展示实例：
```markdown
| 口径项 | 定义 |
|---|---|
| FORMAL单位 | 完整付费入口 |
```

## 2. 指标对齐详情

- 展示方式：五列概览表展示全部指标的基线到FORMAL变化；分类、编号和顺序与阶段2、3一致。
- 必需字段：编号、指标、分类、作用域、基线→FORMAL
- 空值规则：合同指标不得遗漏；没有FORMAL结果时明确显示。
- 展示实例：
```markdown
| 编号 | 指标 | 分类 | 作用域 | 基线→FORMAL |
|---|---|---|---|---|
| M01 | 总RTP | 硬指标 | paid-entry | 不通过 → 通过 |
```

### 2.1 硬指标

- 展示方式：每项硬指标使用独立四级小章节；详细目标、基线、FORMAL、差距和最终门禁合并为一张结构适配表；受统一政策检验时追加合同计划与FORMAL实际样本对照。
- 必需字段：指标编号、指标ID、类型、作用域、业务单位、评价方法、样本资格、样本能力、指标说明、使用场景、目标值含义、基础容差、生效容差、基线值、FORMAL值、最终结果
- 空值规则：任何适用硬指标没有FORMAL结果即无法通过。
- 展示实例：
```markdown
#### M01 总RTP

> 指标编号：M01｜指标ID：core.rtp.total｜类型：硬指标｜作用域：paid-entry
> 业务单位：%｜评价方法：目标区间判定｜样本资格：有效
> 指标说明：衡量玩家每投入100单位投注，长期平均返还多少单位奖金。
> 使用场景：用于确认总体返还水平是否落在权威RTP范围内。
> 目标值含义：统计全部合格完整付费入口的总派奖÷总实际投注。
> 基础容差：0.002｜系数：1｜生效容差：0.002

| 目标下限 | 目标上限 | 单位 | 基线值 | FORMAL值 | 变化/差距 | 最终结果 |
|---|---|---|---|---|---|---|
| 95.8 | 96.2 | % | 95 | 96 | 0 | 门禁：通过 |

> 样本能力：不适用；本指标不受当前统一样本能力政策检验。
```

### 2.2 评分指标

- 展示方式：每项评分指标使用独立四级小章节；分布和复合目标逐项对比基线与FORMAL；受统一政策检验时逐条件组展示合同计划、FORMAL实际、所需、缺口和最终状态。
- 必需字段：指标编号、指标ID、类型、作用域、业务单位、评价方法、样本资格、样本能力、指标说明、使用场景、目标值含义、评分组、权重、基线值、FORMAL值、最终结果
- 有序指标规则：评价方法为一维或分组一维Wasserstein距离时，必须另行展示有序轴、原始轴单位、位置变换和距离单位。
- 空值规则：评分异常或缺失时不得进入综合分。
- 展示实例：
```markdown
#### M08 Feature时长分布

> 指标编号：M08｜指标ID：feature.duration｜类型：评分指标｜作用域：feature
> 业务单位：%（样本占比）｜评价方法：一维Wasserstein距离（考虑有序档位相邻程度）｜样本资格：有效
> 指标说明：衡量完整Feature最终持续了多少次免费旋转。
> 使用场景：用于比较短局、标准长度和长局Feature的出现结构。
> 目标值含义：统计实际免费旋转次数落入对应长度档位的Feature入口比例。
> 评分组：experience｜权重：1｜控制簇：feature-duration
> 有序轴：natural_linear｜原始轴单位：次｜位置变换：identity｜距离单位：normalized_linear_support

| 分布项 | 目标占比 | 基线占比 | FORMAL占比 | 差值（百分点） | 最终结果 |
|---|---|---|---|---|---|
| 12次免费旋转 | 20 | 18 | 19.8 | -0.2 | 得分：96；高度对齐 |

> 样本能力门禁：计划通过｜FORMAL实际通过｜方法：一维Wasserstein距离｜置信水平：99%｜比较噪声预算：0.02｜单侧误差预算：0.01

| 条件组 | 支持项数 | 侧别 | 合同计划 | FORMAL实际 | 所需样本 | 缺口 | 状态 |
|---|---|---|---|---|---|---|---|
| 全部样本 | 5 | 原版 | 50000 | 不适用 | 42000 | 0 | 通过 |
| 全部样本 | 5 | FORMAL | 50000 | 48000 | 42000 | 0 | 通过 |
```

### 2.3 审计指标

- 展示方式：每项审计指标使用独立四级小章节；详细目标与FORMAL审计结果同表展示。审计不计分，但目录声明的缺失或不一致阻塞仍影响FORMAL有效性；不受统一政策检验时不生成空表。
- 必需字段：指标编号、指标ID、类型、作用域、业务单位、评价方法、样本资格、样本能力、指标说明、使用场景、目标值含义、审计要求、FORMAL值、最终结果
- 空值规则：无结果时显示“无审计结果”，但不得改变硬指标或综合分。
- 展示实例：
```markdown
#### M21 最大中奖审计

> 指标编号：M21｜指标ID：core.max_win.audit｜类型：审计指标｜作用域：paid-entry
> 业务单位：倍投注额（x）｜评价方法：审计核对｜样本资格：有效
> 指标说明：核对样本观测最大中奖、规则理论最大中奖、封顶值、触顶次数、实际超限事件和超限处理规则。
> 使用场景：用于确认高倍结果、封顶触发和超限处理符合权威规则。
> 目标值含义：逐字段记录观测最大值、理论最大值、封顶值、触顶次数、超限事件数及超限规则核对状态。
> 审计要求：仅审计｜控制簇：无

| 目标字段 | 目标值 | 单位 | 基线值 | FORMAL值 | 差值 | 最终结果 |
|---|---|---|---|---|---|---|
| 样本观测最大中奖 | 1200 | 倍投注额（x） | — | 1200 | 0 | 审计状态：已审计 |
| 规则理论最大中奖 | 5000 | 倍投注额（x） | — | 5000 | 0 | — |
| 生效封顶值 | 5000 | 倍投注额（x） | — | 5000 | 0 | — |
| 触发封顶次数 | 0 | 次 | — | 0 | 0 | — |
| 实际超限事件次数 | 0 | 次 | — | 0 | 0 | — |
| 超限处理规则核对状态 | 符合 | 状态 | — | 符合 | — | — |

> 样本能力：不适用；本指标不受当前统一样本能力政策检验。
```

## 3. 综合评分汇总

- 展示方式：章节容器，只保留评分组和低分项汇总，不重复逐项指标。
- 必需字段：无独立字段（由3.1至3.2子章节组成）
- 空值规则：综合分无法判定时明确阻塞交付。
- 展示实例：
```markdown
本章只汇总逐项指标小章节已经给出的评分结果。
```

### 3.1 评分组汇总

- 展示方式：先用七列表格展示FORMAL固定评分预算，再用六列表格展示评分组结果，完整说明“作用域实例→评分预算→评分组→综合分”。
- 必需字段：评分预算键、评分组、作用域实例数、作用域聚合、预算得分、档位、指标权重、得分、组权重、有效评分预算数、汇总方法
- 空值规则：无有效评分预算或评分组时写“无”。
- 展示实例：
```markdown
| 评分预算键 | 评分组 | 作用域实例数 | 作用域聚合 | 预算得分 | 档位 | 指标权重 |
|---|---|---|---|---|---|---|
| feature_cycle.duration_distribution | feature_process | 2 | weighted_mean | 90 | 对齐通过 | 1 |

| 评分组 | 得分 | 档位 | 组权重 | 有效评分预算数 | 汇总方法 |
|---|---|---|---|---|---|
| experience | 90 | 对齐通过 | 1 | 5 | 先按score_budget_key聚合作用域，再按指标权重汇总 |
```

### 3.2 低于85分项

- 展示方式：四列表格只列FORMAL低于85分的指标。
- 必需字段：指标、得分、档位、差距
- 空值规则：没有低分项时输出“无”。
- 展示实例：
```markdown
| 指标 | 得分 | 档位 | 差距 |
|---|---|---|---|
| 无 | — | — | — |
```

## 4. 玩法画像与指标覆盖

- 展示方式：章节容器，展示玩法、指标包和覆盖率。
- 必需字段：无独立字段（由4.1至4.3子章节组成）
- 空值规则：覆盖率不足时最终结论不得通过。
- 展示实例：
```markdown
本章证明指标清单覆盖全部必需玩法节点。
```

### 4.1 玩法画像

- 展示方式：玩法摘要、属性和证据分别展示。
- 必需字段：玩法ID、中文名、父节点、状态、作用域、置信状态、属性、值、证据项、证据
- 空值规则：无玩法节点时写“无”并阻塞。
- 展示实例：
```markdown
| 玩法ID | 中文名 | 父节点 | 状态 | 作用域 | 置信状态 |
|---|---|---|---|---|---|
| feature.free-spin | 免费旋转 | paid-entry | 必需 | feature | 高 |

| 玩法ID | 属性 | 值 |
|---|---|---|
| feature.free-spin | spins | 12 |

| 玩法ID | 证据项 | 证据 |
|---|---|---|
| feature.free-spin | 第1项 | rules.md |
```

### 4.2 指标包匹配

- 展示方式：五列表格列出合同指标的Owner、类型、作用域和状态。
- 必需字段：指标ID、Owner、类型、作用域、状态
- 空值规则：必需指标缺Owner时阻塞。
- 展示实例：
```markdown
| 指标ID | Owner | 类型 | 作用域 | 状态 |
|---|---|---|---|---|
| core.rtp.total | core | hard | paid-entry | 必需 |
```

### 4.3 覆盖率

- 展示方式：两列表格展示玩法与指标覆盖结果。
- 必需字段：项目、结果
- 空值规则：必需覆盖率必须为100%。
- 展示实例：
```markdown
| 项目 | 结果 |
|---|---|
| 指标可测率 | 1 |
```

## 5. 参数变化

- 展示方式：参数变化摘要和影响指标详情分开展示。
- 必需字段：参数、原值、对齐值、变化、授权、控制簇、运行风险、指标项、影响指标
- 空值规则：未授权参数不得出现在变化清单。
- 展示实例：
```markdown
| 参数 | 原值 | 对齐值 | 变化 | 授权 | 控制簇 | 运行风险 |
|---|---|---|---|---|---|---|
| feature.weight | 1 | 2 | 1 | 已授权 | feature | 低 |

| 参数 | 指标项 | 影响指标 |
|---|---|---|
| feature.weight | 第1项 | core.rtp.total |
```

### 5.1 权限与玩法边界确认

- 展示方式：三列表格确认禁止类别未修改。
- 必需字段：边界、结论、依据
- 空值规则：任一禁止类别被修改时淘汰候选；只有越过禁止边界才能完成的指标必须自动豁免并展示证据。
- 展示实例：
```markdown
| 边界 | 结论 | 依据 |
|---|---|---|
| 状态机 | 未修改 | 禁止类别 |
```

## 6. CALIBRATION过程

- 展示方式：章节容器，展示预算、候选和循环结束原因。
- 必需字段：无独立字段（由6.1至6.3子章节组成）
- 空值规则：缺少预算密封或候选归档时FORMAL无效。
- 展示实例：
```markdown
本章记录受控参数搜索，不重复指标详细结果。
```

### 6.1 搜索与预算

- 展示方式：预算、可达性、预算上限和连续执行政策分别使用两列表格。
- 必需字段：预算项、可达性项、上限项、连续执行政策项、值
- 空值规则：FORMAL通过前不得因预算或候选次数停止；结构不可达后不得继续仅靠预算扩张。
- 展示实例：
```markdown
| 预算项 | 值 |
|---|---|
| calibration_samples | 100000 |

| 可达性项 | 值 |
|---|---|
| status | 可达 |

| 上限项 | 值 |
|---|---|
| enabled | 是 |

| 连续执行政策项 | 值 |
|---|---|
| interrupt_user_during_execution | 否 |
```

### 6.2 候选演进

- 展示方式：候选摘要与参数详情分开展示。
- 必需字段：候选、父候选、硬指标、综合分、样本、风险、晋级/淘汰原因、stage3_gate hash、状态、参数项、参数摘要
- 空值规则：候选未绑定有效阶段3门禁时不得进入FORMAL。
- 展示实例：
```markdown
| 候选 | 父候选 | 硬指标 | 综合分 | 样本 | 风险 | 晋级/淘汰原因 | stage3_gate hash | 状态 |
|---|---|---|---|---|---|---|---|---|
| C01 | baseline | 通过 | 90 | 100000 | 低 | 最优 | abc… | 冻结 |

| 候选 | 参数项 | 参数摘要 |
|---|---|---|
| C01 | weight | 2 |
```

### 6.3 循环结束原因

- 展示方式：一句话说明搜索循环结束的真实原因。
- 必需字段：循环结束原因
- 空值规则：只允许“FORMAL通过”或“结构不可达证据已密封”；不得使用预算用完或候选次数达到上限。
- 展示实例：
```markdown
循环结束原因：全部硬指标通过且综合分达到要求。
```

## 7. FORMAL验收

- 展示方式：三列表格核对计划要求与实际执行。
- 必需字段：项目、计划/要求、实际结果
- 空值规则：执行路径、样本或独立性不合格时FORMAL无效。
- 展示实例：
```markdown
| 项目 | 计划/要求 | 实际结果 |
|---|---|---|
| 执行路径 | 仅Python | python |
```

### 7.1 独立性证明

- 展示方式：独立性检查和输入Hash分别展示。
- 必需字段：检查项、要求、结果、输入对象、SHA-256
- 空值规则：任一独立性检查失败即FORMAL无效。
- 展示实例：
```markdown
| 检查项 | 要求 | 结果 |
|---|---|---|
| 候选冻结 | FORMAL前固定 | 通过 |

| 输入对象 | SHA-256 |
|---|---|
| candidate | abc… |
```

## 8. 豁免、不可达与阻塞

- 展示方式：豁免摘要、批准详情和证据详情分开展示。
- 必需字段：指标实例、原因码、状态、原因、授权来源、批准状态、批准项、证据项、内容
- 空值规则：无相关项时保留“无”行。
- 展示实例：
```markdown
| 指标实例 | 原因码 | 状态 | 原因 | 授权来源 | 批准状态 |
|---|---|---|---|---|---|
| 无 | — | — | — | — | — |

| 指标实例 | 批准项 | 内容 |
|---|---|---|
| 无 | — | — |

| 指标实例 | 证据项 | 内容 |
|---|---|---|
| 无 | — | — |
```

## 9. 最终交付建议

- 展示方式：一段文字给出是否进入阶段5。
- 必需字段：无独立字段（由正文结论组成）
- 空值规则：FORMAL无效时必须等待补证。
- 展示实例：
```markdown
进入阶段5封存并完成交付。
```

## 10. 版本、Hash与复算

- 展示方式：两列表格列出全部输入和产物Hash。
- 必需字段：对象、SHA-256
- 空值规则：关键对象Hash缺失即交付校验失败。
- 展示实例：
```markdown
| 对象 | SHA-256 |
|---|---|
| metric_contract.json | abc… |
| ordered_distance_policy | def… |
```

### 10.1 复算命令

- 展示方式：bash代码块提供确定性复算命令。
- 必需字段：无独立字段（由bash代码块组成）
- 空值规则：命令必须使用原始认证脚本或已通过自动等价门禁的派生Python路径。
- 展示实例：
```markdown
<python_bin> score_alignment.py ...
```

## 附录索引

- 展示方式：项目列表链接阶段1至4机器产物。
- 必需字段：无独立字段（由项目列表组成）
- 空值规则：关键目录缺失即报告不完整。
- 展示实例：
```markdown
- 阶段2：指标合同、扩展与豁免
- 阶段4：FORMAL机器结果
```
