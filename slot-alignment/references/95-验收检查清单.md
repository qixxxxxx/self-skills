# FORMAL 与交付前检查

- [ ] 作用域、目标 RTP 和全部上游 hash 已密封。
- [ ] 付费入口链、金额守恒、自然/定向样本分流正确。
- [ ] 阶段1已完成唯一一次 server flow 认证批次，脚本与当前 server/Runtime一致，观测增强未改变逻辑。
- [ ] 必需玩法语义有唯一 Owner；玩法覆盖率 100%。
- [ ] 每个玩法语义的`metric_requirements`均被真实`capability_ids`承接；无自由文本匹配、孤儿玩法或未知画像属性。
- [ ] Core 与专项必需指标完整；指标可测率 100%。
- [ ] `semantic_contract_validation.py`通过；空画像、未知/缺失属性、漏Core、漏专项指标、目录语义篡改、包映射和覆盖率自报均已机器阻塞。
- [ ] `game_profile.scope.mode_contract`与`metric_contract.scope.mode_contract`逐字段一致；玩家可选Feature模式和Normal/Ante/Bonus Bet等付费配置均按独立任务固定，未跨模式或跨实际经济投注口径混样。
- [ ] 所有不适用指标仍保留合同项，并绑定原因码和可追溯证据。
- [ ] 有可见符号盘面时已加载盘面多样性指标；有标准符号结算时已加载中奖结构多样性指标，或存在逐项“不适用”证据。
- [ ] 入口初始盘、Feature初始盘、演化后完整盘及Cascade新补入格未被无证据混合。
- [ ] Cascade存在稳定轴列或区域分区时，补入格数、给定数量后的分区计数向量和补入符号分别由唯一Owner承接；0补入、单分区及确定性唯一向量未伪装成评分。
- [ ] 符号顺序、有效格、符号角色、数量/堆叠档位和中奖规模维度均在候选出现前密封。
- [ ] 评价合同、基础容差、容差系数、生效容差、政策 hash、权重、样本资格和边界在看候选前密封。
- [ ] 每项评分指标具有固定`score_budget_key`；同指标多作用域先聚合再计一次权重，未按实例数量自然增权。
- [ ] 派生指标、边际重复项和完整分布摘要均为权重0的审计；同一`semantic_variable_id`没有多个Primary评分Owner。
- [ ] 所有活动条件组指标均有可复算`conditional_group_weight_binding`；退化组已移除，原版暴露或来源指标实例及目标证据hash有效，未使用候选频率。
- [ ] 位置角色残差逐组密封完整位置域基线；只有存在选择自由度的组活动，基线与残差相加后仍为合法概率。Respin仅向Persistent边际化，未反向恢复按步骤分布；Walking Wild完整位置对域、唯一`pair_id`和数量转移组一致，配对残差使用分组平均绝对误差，且每组总和、起点行和、终点列和均为0，未停用位置份额或位置角色Owner。
- [ ] Hold & Spin重转资源规则审计逐项核对初始次数、消耗、续命触发、reset/add/replace、续命数量、上限时点和退出；步骤当前占用边际覆盖全部实际执行步骤，推进条件组权重从同实例原版边际精确复制；入口、步骤当前、条件推进和终局四个数量Owner没有互相替代或与通用持久数量重复评分。
- [ ] 多格Mystery/批量变形已声明目标分配作用域与规则；目标一致性残差按真实变换格数和无序来源对分组、每事件等权、扣除候选自身目标边际独立基线，未保存完整目标向量或重复位置/回报Owner。
- [ ] Wild存在可证明的参与格身份时，实际辅助格数按步骤去重并与盘面Wild总数、扩展新增格数分开；无法证明身份时已阻塞而非猜测。
- [ ] 通用`state`作用域仅使用结果生成前已知、互斥且完备的数学状态；未按中奖、回报、最终深度或其他结果后字段拆分，也未复制已有专用维度扩大评分预算。
- [ ] Value Symbol↔倍率和Cascade↔倍率的单Owner派生均有完整画像绑定及合同逐字段副本；派生目标、Cascade步骤暴露权重和非零依赖残差均可从主Owner精确复算，未形成双向派生。
- [ ] 阶段3固定scorecard和中文报告已生成；报告由当前合同与机器scorecard确定性生成，不是手写或候选临时结果。
- [ ] 任务目录符合`<game_code>/alignments/<mode>/<task_id>/`，没有Runtime版本或RTP Group中间目录。
- [ ] `artifacts/`只包含机器JSON；全部中文Markdown位于同一`交付物/报告文档/rv####/`且没有`stages/`、`post-delivery/`或`dv####/`报告子目录。
- [ ] `交付物/runtime/`包含FORMAL实际使用的Runtime四件套；`game_core.json.meta.version=task_id`，RTP路由仅含Group 1。
- [ ] 阶段1至5模板每章均有展示方式、必需字段及顺序、空值规则和Markdown实例；实报章节顺序一致、章节非空、字段齐全、表头名称及顺序一致、无占位符，且确定性重渲染逐字一致。
- [ ] 阶段2、3、4每项指标均说明衡量内容、使用场景和目标统计口径；目标值使用百分比、倍投注额、次数等业务单位。
- [ ] `references/metrics/指标汇总.md`由当前JSON目录确定性生成，与目录指纹一致；所有指标恰好出现一次，分类、画像、语义、单位、方法、缺失处理和关系完整。
- [ ] 所有分布项均展示真实业务区间或多维组合，不存在“联合桶01、回报桶01、取值桶01、时长桶01、分布项01”等编号占位。
- [ ] 总变差只用于归一分布；有序次数/长度/格数使用真实线性`bin_positions`与密封跨度，回报/奖值/倍率/组合容量使用政策固定`log10(1+x)`、固定变换单位和尺度1且未被极值跨度稀释；Collect、中奖规模和持久状态轴语义与画像一致；条件分布和分组残差已密封原版侧`group_weights`且未使用候选频率。
- [ ] 有序距离政策路径与SHA-256有效，合同内政策字段、活动有序指标实例清单及其SHA-256均与真实来源一致；log轴没有负位置，候选未决定变换、尺度或支持集。
- [ ] 顶层评分组权重由版本化政策按活动语义组确定性生成，政策路径与SHA-256有效；指标数量、作用域数量和候选结果未改变组预算。
- [ ] `stage3_gate.json`为`通过`且`stage4_allowed=true`；所有阶段4候选绑定该门禁hash。
- [ ] 权威总 RTP 系数为 1.0 且未扩大目标区间；旧任务未回溯套用新政策。
- [ ] 组件 RTP 目标由原版贡献占比映射权威总 RTP；占比合计为1，目标逐端点之和还原权威总 RTP，未直接使用原版绝对 RTP。
- [ ] 配置异常已排除；不可达结论有结构证据。
- [ ] 若已密封结构不可达，`budget_expansion_allowed=false`且未继续单纯追加预算。
- [ ] 所有豁免均由用户批准且 hash 仍有效。
- [ ] 只修改授权数值参数；候选合法且已冻结。
- [ ] FORMAL 种子与样本独立，尝试次数符合密封计划。
- [ ] 阶段2至阶段5、CALIBRATION和FORMAL只通过`python_bin`执行已认证`.py`脚本，所有服务端/JVM调用数均为0；FORMAL绑定阶段1认证证据hash。
- [ ] 机器评分可复算；Markdown 与 JSON 一致。
- [ ] 200x 以上、最大中奖、封顶和溢出均完成审计。
- [ ] 交付manifest以任务根目录为基准，阶段机器JSON、阶段1至4中文报告和FORMAL Runtime四件套的路径与SHA-256全部有效。

任一必需项失败时不得显示普通`通过`或发布合格交付状态。

上述清单只约束阶段1至5封存。封存后单次 server flow 硬指标审计无论成功或失败都不得改写本清单结论，只在独立报告中警告。
