# metric_contract 1.4存储与兼容规范

## 1. 目标与边界

本规范定义 `metric_contract.schema_version=1.4` 的磁盘结构、统一加载方式、兼容边界和性能验收。目标是在不减少指标、条件组、证据绑定、评分门禁或样本能力检查的前提下，消除目录静态字段和大型条件对象的重复展开，使后续已登记玩法组合只需提供玩法画像和任务数据，不再修改共享 Skill 代码。

本规范只约束共享`slot-alignment` Skill的合同存储与加载。实施或验证时不得覆盖历史任务产物，不运行游戏跑参、JVM、服务端或Runtime，不执行配置同步、发布或Git提交。

## 2. 兼容边界

### 2.1 metric_contract 1.3

- `assets/templates/artifacts/02-metric-matching/metric_contract.json`是政策链展开1.3工作模板；它用于逐步应用政策和生成完整内存语义，不是新任务的阶段2最终固定产物。
- 已密封的 1.3 合同保持只读，文件内容、SHA-256 和历史复算语义不变。
- 加载 1.3 时不补写、不迁移、不重排字段；加载器只返回原合同的内存对象。
- v2.5～v2.9及v3.2历史重放仍遵守既有`--historical-replay`门禁，不因1.4获得新的写入或重新进入阶段2权限。
- 1.3 的评分、报告和交付 Hash 继续绑定原始合同文件字节。

### 2.2 metric_contract 1.4

- 1.4 是新任务阶段2的最终固定紧凑磁盘合同；权威 Hash 绑定紧凑主合同及其全部外部机器数据引用。
- 现有消费者不得自行判断紧凑字段或读取外部张量，必须通过统一 `contract_io` 加载。
- 加载器生成的展开内存视图保持现有消费者所需的 `metrics[]` 结构，但展开视图不是新的密封产物，不得替代紧凑主合同计算来源 Hash。
- 1.4 不允许静默回退为 1.3 写法；违反继承、引用或稳定 ID 规则时 fail fast。

## 3. v1.4紧凑合同结构

顶层继续保留任务身份、作用域、目录绑定、政策、覆盖率、Owner 分区、预算与 FORMAL 计划。指标实例改为紧凑记录，并新增 `contract_storage`：

```json
{
  "schema_version": "1.4",
  "report_contract_version": "slot-alignment.reports.v3.3",
  "task_id": "aln-normal-YYYYMMDD-NNN",
  "catalogs": {
    "metric_catalog_version": "2.0",
    "metric_catalog_sha256": "<64hex>"
  },
  "contract_storage": {
    "layout": "compact_metric_instances_v1",
    "catalog_inheritance_version": "1",
    "instance_id_algorithm": "metric-instance-id-v1",
    "external_data_schema_version": "slot-alignment.contract-data.v1",
    "canonical_json": "utf8-sort_keys-compact-v1"
  },
  "metrics": [
    {
      "instance_id": "mi_4c01...",
      "metric_id": "cascade.step_return_distribution_by_depth",
      "source_node_ids": ["cascade-main"],
      "instance_dimensions": {"component": "base", "state": "base_game"},
      "scope": "normal:g1|component=base|mode=normal|state=base_game",
      "status": "适用",
      "target": {"$data_ref": "target:mi_4c01..."},
      "score_profile_patch": {"group_weights": {"$data_ref": "group_weights:mi_4c01..."}},
      "sample_capability": {"status": "阻塞", "detail_ref": {"$data_ref": "sample_capability:mi_4c01..."}}
    }
  ],
  "external_data": [
    {
      "data_id": "metric-tensors",
      "path": "metric_contract_data/metric-tensors.json",
      "schema_version": "slot-alignment.contract-data.v1",
      "sha256": "<64hex>",
      "record_count": 123,
      "instance_count": 45
    }
  ]
}
```

`metrics[]` 只保存实例身份、作用域、任务状态、任务证据、任务目标、政策结果和相对目录默认值的覆盖项。任何会影响评分或门禁的任务专属值都不得依赖加载时猜测。

## 4. 目录字段继承

以下字段默认由当前合同绑定的指标目录条目按 `metric_id` 继承，主合同不重复复制：

- 基础身份与阅读：`name_zh`、`category`、`display_order`、`display`。
- 测量语义：`measurement`、`unit`、`sample_unit`、`condition_on`、`normalization`、`return_denominator`、`missing_policy`。
- 适用与实例规则：`owner`、`capability_ids`、`profile_match`、`applicability_rule`、`scope_template`、`inapplicability_reason_codes`、`conditional_derivation_requirements`。
- Owner 与评分语义：`kind`、`semantic_role`、`semantic_group`、`semantic_variable_id`、`relationships`、`score_group`、`score_budget_key`、`scope_aggregation`、`default_weight`。
- 评价默认值：目录中的 `hard_gate_profile`、`audit_profile` 和 `score_profile` 基线。

继承使用“目录基线 + 实例覆盖”的确定性深合并：

1. 字典递归合并；实例值覆盖目录值。
2. 数组和标量整体替换，不按位置合并。
3. `null` 是显式值，不代表删除；需要删除目录可选字段时使用受控 `remove_fields`。
4. `metric_id` 未命中、命中不唯一、目录 Hash 不一致或覆盖受保护字段时直接失败。

以下字段必须保留在实例或任务合同中，不得仅从目录继承：

- `instance_id`、`metric_id`、`source_node_ids`、`instance_dimensions`、`scope`、`status`。
- `target`、`target_evidence`、`target_derivation`。
- `sealed_event_set_id/path/sha256/count`、`scope_weight`、`weight`、`score_weight`。
- 实际生效的 `hard_gate_profile`、`audit_profile`、`score_profile` 覆盖结果。
- `conditional_group_weight_binding`、Owner 派生绑定、退化与不适用证据。
- `sample_capability_input` 与样本能力结论或其明细引用。

## 5. 展开内存视图

统一加载器对 1.4 执行以下顺序：

1. 严格读取主合同并拒绝重复 JSON 键。
2. 校验版本、目录版本和目录 SHA-256。
3. 校验所有外部数据引用的安全相对路径、Schema、SHA-256、记录数和实例数。
4. 以目录条目建立不可变基线。
5. 按稳定 `instance_id` 应用实例覆盖和外部数据切片。
6. 展开样本能力明细，并保留主合同中的摘要字段。
7. 运行结构校验，返回与 1.3 消费者字段形状兼容的 `dict`。

展开视图必须满足：

- `metrics[]` 排序与紧凑合同一致。
- 同一输入、目录与外部数据得到字节级确定的规范化展开 JSON。
- 展开不得读取候选测量结果、环境默认值、当前时间或未绑定文件。
- 加载器提供来源信息，区分 `source_contract_sha256` 与可选的 `expanded_view_sha256`。

## 6. 条件张量结构

大型条件分布使用结构化张量，禁止继续用数万条 `组ID::档位` 字符串键重复存储相同轴：

```json
{
  "tensor_schema": "conditional_distribution_tensor.v1",
  "instance_id": "mi_4c01...",
  "group_axes": [
    {"name": "cascade_depth", "values": [1, 2, 3]},
    {"name": "capacity", "values": [12, 18]}
  ],
  "value_axis": {"name": "return_multiple", "values": [0, 0.2, 1, 5]},
  "active_groups": [[0, 0], [0, 1], [1, 0]],
  "probabilities": [
    [0.8, 0.1, 0.08, 0.02],
    [0.7, 0.15, 0.1, 0.05],
    [0.6, 0.2, 0.15, 0.05]
  ],
  "group_weights": [0.5, 0.3, 0.2],
  "sample_counts": [120000, 72000, 48000]
}
```

约束：

- 轴名、轴值和单位显式保存；轴值顺序固定且不得依赖对象键顺序。
- `active_groups` 使用轴下标元组；不得保存 0 权重或退化组。
- 行数必须与活动组、组权重和逐组样本数一致。
- 每个条件概率行按指标方法校验归一化或残差范围。
- 展开键由目录声明的键编码器确定性生成；编码规则版本进入张量 Schema。
- `display.item_labels`、`score_profile.group_weights`、`bin_positions_by_group`、目标和样本能力逐组字段复用同一轴定义，不得各自复制组键全集。

## 7. 外部数据引用与 Hash

大型张量和样本缺口明细可以存放在主合同同级子目录。每项引用必须包含：

- `data_id`：合同内唯一稳定 ID。
- `path`：相对主合同目录的 POSIX 相对路径。
- `schema_version`：外部数据 Schema。
- `sha256`：文件原始字节 SHA-256。
- `record_count`、`instance_count`：用于快速拒绝截断或串包。

安全规则：

- 禁止绝对路径、`..`、空路径、符号链接逃逸和解析后越出主合同目录。
- 同一路径只能绑定一个 SHA-256；同一 `data_id` 只能出现一次。
- 未声明的文件不能被 `$data_ref` 读取；未被引用的声明默认报错。
- 外部数据变化会使合同整体绑定失效，不能只更新主合同中的 Hash 后继续使用旧报告。

合同内容身份定义为：主合同原始 SHA-256 加按 `data_id` 排序后的 `(data_id, schema_version, sha256, record_count, instance_count)` 清单。交付和跨阶段校验同时保存主合同 Hash 与该内容身份 Hash。

## 8. 指标实例稳定 ID

算法 ID 固定为 `metric-instance-id-v1`：

1. 取 `metric_id`。
2. 对 `source_node_ids` 去重后按 Unicode 码点排序。
3. 对 `instance_dimensions` 使用 UTF-8、键排序、无多余空白的规范 JSON。
4. 拼接规范对象：

```json
{"instance_dimensions":{},"metric_id":"...","source_node_ids":[]}
```

5. 计算 SHA-256，实例 ID 为 `mi_` 加前 24 个十六进制字符。

完整摘要仍应在加载时复算，以检测理论上的短前缀碰撞；同一合同内短 ID 冲突直接失败，不自动延长。`scope` 是上述身份维度的确定性投影和校验字段，不参与 ID，避免仅字符串格式变化导致实例身份漂移。

## 9. 样本缺口摘要与明细

主合同只保留可用于阶段门禁和报告摘要的字段：

```json
{
  "sample_capability_policy": {
    "status": "阻塞",
    "metric_instance_count": 74,
    "blocked_instance_count": 12,
    "gap_record_count": 9409,
    "original_total_gap": 123456,
    "formal_total_gap": 0,
    "gap_detail_ref": {"$data_ref": "sample-gaps"}
  }
}
```

详细缺口逐实例、逐侧、逐活动组保存到独立数据文件，至少包含 `instance_id`、`side`、`conditional_group_index`、`required_count`、`actual_count`、`gap`、`status` 和输入摘要。加载器可以为旧消费者展开原 `sample_capability` 结构，但主合同报告门禁优先读取摘要，只有明细页面或复算时加载详细记录。

## 10. 消费者统一接入

消费者接入顺序固定为：

1. 新增 `scripts/contract_io.py`，提供严格读取、1.3 透传、1.4 展开、内容身份计算和外部引用校验。
2. 政策脚本先改为使用加载器，但输出阶段仍保持其输入版本；1.4 写入器完成后才产生紧凑输出。
3. 语义校验、评分、阶段 2/3/4 报告、阶段转换、产物校验和交付封存依次迁移。
4. 禁止消费者保留第二套局部展开逻辑；所有兼容分支集中在 `contract_io`。
5. 来源 Hash 继续绑定磁盘主合同；需要比较展开语义时另用 `expanded_view_sha256`，两者不得混用。

命令行接口继续接收 `--contract <metric_contract.json>`，不要求调用者手工传目录或外部数据路径；加载器从合同绑定和 Skill 根目录解析。

## 11. 缓存键与失效

展开缓存键至少包含：

```text
loader_version
main_contract_sha256
contract_content_identity_sha256
metric_catalog_sha256
catalog_inheritance_version
external_data_schema_versions
```

缓存只存可重建展开结果，不是权威产物。任一键变化全部失效；缓存损坏时删除单个明确缓存文件并重建，不得把缓存失败降级为跳过 Hash 校验。并发写入使用临时文件加原子替换。

## 12. 前置能力检查

通用实例编译前必须输出机器可读能力报告，逐玩法节点和逐必需指标检查：

- 目录命中、实例化规则和 Owner 唯一性。
- 统计脚本是否提供指标所需事件、字段、状态拆分和逐组样本计数。
- 目标证据、实际业务轴、条件组绑定和样本能力输入是否齐全。
- 授权参数是否能控制目标语义，结构不可达是否已有证据。

任何必需能力缺失必须在阶段 2 大合同生成前停止，并列出缺失指标、玩法节点、条件组和补充接口。不得先生成数万条件组后才发现消费者不支持。

## 13. 性能验收基线

以 Buffalo Normal 当前 1.3 合同作为压力基线：约 40 MB、136 个实例、5,464 个条件组、9,409 条样本缺口；最大实例约 31,344 个目标键。

v1.4 最低验收：

- 紧凑主合同不超过当前 1.3 主合同的 40%，即约 16 MB；主合同加外部数据总量不超过当前文件的 65%，即约 26 MB。
- 删除目录静态字段重复后不得再在实例中复制完整 `display`、测量说明或目录评分基线。
- 冷加载并展开压力基线的目标为 5 秒内完成；同进程缓存命中目标为 0.5 秒内完成。
- 语义校验、评分和报告结果与等价 1.3 展开合同一致；允许的差异仅限 Schema、存储引用和来源 Hash 字段。
- 104 个目录指标、24 个指标包及全部已登记玩法组合必须通过实例编译、Schema、Hash、Owner、政策和报告自测。

性能阈值以固定机器环境的多次测量中位数验收；若未达到，先定位 I/O、重复展开或重复 Hash，不得通过减少指标、条件组或验证项目达标。

## 14. 停止条件

- 目录字段无法确定性继承，或同一 `metric_id` 存在不唯一语义。
- 条件数据无法表示为有明确轴和单位的张量。
- 外部数据无法绑定安全相对路径、Schema 和 SHA-256。
- 展开视图与等价 1.3 合同产生评分、门禁或报告语义差异。
- 新玩法需要目录未登记的数学语义、事件字段或 Owner；此时按指标扩展流程请求用户批准，不在游戏任务中临时写特例。
