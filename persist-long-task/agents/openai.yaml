interface:
  display_name: "长任务持久化"
  short_description: "通过检查点和恢复门禁持续执行，状态查询也不中断"
  default_prompt: "使用 $persist-long-task 为这个长任务建立可恢复状态并持续执行，状态查询只做中间汇报，不停止原任务。"
policy:
  allow_implicit_invocation: false
